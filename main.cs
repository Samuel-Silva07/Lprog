using System;

class Aula1 {
  double f(double x,
          double a,
          double b,
          double c){ 
    return y = a*Math.Pow(x,2) + b*x + c;
    return y;
  }
public static void Main (string[] args)  {
  int v = 0;
  Console.writeline (v);
  double y = f(1,2,3,4);
  Console.WriteLine(y);
  double  x = double.Parse(
    Console.ReadLine ()
    );
    y = false(x,2,3,4);
  Console.WriteLine (y);  
  }
}